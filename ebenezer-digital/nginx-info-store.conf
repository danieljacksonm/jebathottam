# Drop on VPS as /etc/nginx/sites-available/ebenezer-info-store
# sudo ln -sf /etc/nginx/sites-available/ebenezer-info-store /etc/nginx/sites-enabled/
# sudo nginx -t && sudo systemctl reload nginx

# HTTP → HTTPS  (.info journal)
server {
    listen 80;
    listen [::]:80;
    server_name ebenezerdigital.info www.ebenezerdigital.info;
    location /.well-known/acme-challenge/ {
        root /var/www/html;
    }
    location / {
        return 301 https://$host$request_uri;
    }
}

# HTTPS  (.info journal)
server {
    listen 443 ssl http2;
    listen [::]:443 ssl http2;
    server_name ebenezerdigital.info www.ebenezerdigital.info;
    client_max_body_size 20M;

    ssl_certificate /etc/letsencrypt/live/ebenezerdigital.info/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/ebenezerdigital.info/privkey.pem;
    include /etc/letsencrypt/options-ssl-nginx.conf;
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;

    location / {
        proxy_pass http://127.0.0.1:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
        proxy_read_timeout 86400;
    }
}

# HTTP → HTTPS (journal subdomain)
server {
    listen 80;
    listen [::]:80;
    server_name journal.ebenezerdigital.info www.journal.ebenezerdigital.info;
    location /.well-known/acme-challenge/ {
        root /var/www/html;
    }
    location / {
        return 301 https://$host$request_uri;
    }
}

# HTTPS (journal) — sudo certbot --nginx -d journal.ebenezerdigital.info -d www.journal.ebenezerdigital.info
server {
    listen 443 ssl http2;
    listen [::]:443 ssl http2;
    server_name journal.ebenezerdigital.info www.journal.ebenezerdigital.info;
    client_max_body_size 20M;

    ssl_certificate /etc/letsencrypt/live/journal.ebenezerdigital.info/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/journal.ebenezerdigital.info/privkey.pem;
    include /etc/letsencrypt/options-ssl-nginx.conf;
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;

    location / {
        proxy_pass http://127.0.0.1:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
        proxy_read_timeout 86400;
    }
}

# HTTP → HTTPS (news channel subdomain)
server {
    listen 80;
    listen [::]:80;
    server_name news.ebenezerdigital.info www.news.ebenezerdigital.info;
    location /.well-known/acme-challenge/ {
        root /var/www/html;
    }
    location / {
        return 301 https://$host$request_uri;
    }
}

# HTTPS (news) — sudo certbot --nginx -d news.ebenezerdigital.info -d www.news.ebenezerdigital.info
server {
    listen 443 ssl http2;
    listen [::]:443 ssl http2;
    server_name news.ebenezerdigital.info www.news.ebenezerdigital.info;
    client_max_body_size 20M;

    ssl_certificate /etc/letsencrypt/live/news.ebenezerdigital.info/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/news.ebenezerdigital.info/privkey.pem;
    include /etc/letsencrypt/options-ssl-nginx.conf;
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;

    location / {
        proxy_pass http://127.0.0.1:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
        proxy_read_timeout 86400;
    }
}

# HTTP → HTTPS (AI)
server {
    listen 80;
    listen [::]:80;
    server_name ai.ebenezerdigital.com www.ai.ebenezerdigital.com;
    location /.well-known/acme-challenge/ {
        root /var/www/html;
    }
    location / {
        return 301 https://$host$request_uri;
    }
}

# HTTPS (AI) — sudo certbot --nginx -d ai.ebenezerdigital.com -d www.ai.ebenezerdigital.com
server {
    listen 443 ssl http2;
    listen [::]:443 ssl http2;
    server_name ai.ebenezerdigital.com www.ai.ebenezerdigital.com;
    client_max_body_size 20M;

    ssl_certificate /etc/letsencrypt/live/ai.ebenezerdigital.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/ai.ebenezerdigital.com/privkey.pem;
    include /etc/letsencrypt/options-ssl-nginx.conf;
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;

    location / {
        proxy_pass http://127.0.0.1:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
        proxy_read_timeout 86400;
    }
}

# HTTP  (.store)
server {
    listen 80;
    listen [::]:80;
    server_name ebenezerdigital.store www.ebenezerdigital.store;
    client_max_body_size 20M;

    location / {
        proxy_pass http://127.0.0.1:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
        proxy_read_timeout 86400;
    }
}
