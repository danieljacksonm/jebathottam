# Ebenezer Digital — Environment Variables
JWT_SECRET=change-this-to-a-long-random-string
NEXT_PUBLIC_SITE_URL=https://ebenezar.yegova.store

# Admin login password (default if unset: admin123)
ADMIN_DEFAULT_PASSWORD=admin123

# Set to 1 once to reset admin password to ADMIN_DEFAULT_PASSWORD, then remove it
# ADMIN_FORCE_PASSWORD_RESET=1
