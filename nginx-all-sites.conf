# Copy to VPS: sudo cp nginx-all-sites.conf /etc/nginx/sites-available/all-sites
# Enable:      sudo ln -sf /etc/nginx/sites-available/all-sites /etc/nginx/sites-enabled/
# Test/reload: sudo nginx -t && sudo systemctl reload nginx
# HTTPS:       sudo certbot --nginx -d ebenezerdigital.com -d www.ebenezerdigital.com -d ebenezerdigital.info -d www.ebenezerdigital.info -d ebenezerdigital.store -d www.ebenezerdigital.store -d ebenezerdigital.net -d www.ebenezerdigital.net

# ==================== EBENEZER DIGITAL (main domain + old subdomain) ====================
# After DNS + SSL work, you can redirect ebenezar.yegova.store → ebenezerdigital.com
server {
    listen 80;
    server_name ebenezerdigital.com www.ebenezerdigital.com ebenezar.yegova.store;
    client_max_body_size 50M;

    location / {
        proxy_pass http://127.0.0.1:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
        proxy_read_timeout 86400;
    }
}

# ==================== EBENEZER BLOG (.info) ====================
# Same app on port 3000. Middleware sends / → /blog on this host.
server {
    listen 80;
    server_name ebenezerdigital.info www.ebenezerdigital.info;
    client_max_body_size 20M;

    location / {
        proxy_pass http://127.0.0.1:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
        proxy_read_timeout 86400;
    }
}

# ==================== EBENEZER STORE (.store) ====================
# Same app on port 3000. Middleware sends / → /products on this host.
# Keep HTTP (not a 301 to .com) so Let's Encrypt can issue SSL.
server {
    listen 80;
    server_name ebenezerdigital.store www.ebenezerdigital.store;
    client_max_body_size 20M;

    location / {
        proxy_pass http://127.0.0.1:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
        proxy_read_timeout 86400;
    }
}

# ==================== EBENEZER PRODUCTS (physical compare) ====================
# DNS: products.ebenezerdigital.com → VPS. Middleware redirects / → /catalog
# Note: This is NOT ebenezerdigital.store (digital kits).
server {
    listen 80;
    server_name products.ebenezerdigital.com www.products.ebenezerdigital.com;
    client_max_body_size 20M;

    location / {
        proxy_pass http://127.0.0.1:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
        proxy_read_timeout 86400;
    }
}

# ==================== EBENEZER TOOLS (software compare) ====================
server {
    listen 80;
    server_name tools.ebenezerdigital.com deals.ebenezerdigital.com;
    client_max_body_size 20M;

    location / {
        proxy_pass http://127.0.0.1:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
        proxy_read_timeout 86400;
    }
}

# ==================== EBENEZER AI ====================
# Middleware rewrites / → /ai
server {
    listen 80;
    server_name ai.ebenezerdigital.com www.ai.ebenezerdigital.com;
    client_max_body_size 20M;

    location / {
        proxy_pass http://127.0.0.1:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
        proxy_read_timeout 86400;
    }
}

# ==================== EBENEZER SAAS ====================
# Middleware rewrites / → /saas
server {
    listen 80;
    server_name saas.ebenezerdigital.com www.saas.ebenezerdigital.com;
    client_max_body_size 20M;

    location / {
        proxy_pass http://127.0.0.1:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
        proxy_read_timeout 86400;
    }
}

# ==================== EBENEZER DISCOVER ====================
# Middleware rewrites / → /discover
server {
    listen 80;
    server_name discover.ebenezerdigital.com www.discover.ebenezerdigital.com;
    client_max_body_size 20M;

    location / {
        proxy_pass http://127.0.0.1:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
        proxy_read_timeout 86400;
    }
}

# ==================== EBENEZER DIGITAL NETWORK (.net) ====================
# Free tools platform — same Next.js app; middleware rewrites to /network
server {
    listen 80;
    server_name ebenezerdigital.net www.ebenezerdigital.net;
    client_max_body_size 20M;

    location / {
        proxy_pass http://127.0.0.1:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
        proxy_read_timeout 120;
    }
}

# ==================== MINISTRY PLATFORM ====================
server {
    listen 80;
    server_name demo.jesusisthewayjebathottam.com;
    client_max_body_size 50M;

    location / {
        proxy_pass http://127.0.0.1:3001;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
        proxy_read_timeout 86400;
    }
}

# ==================== SRI KRISHNA MOBILES ====================
server {
    listen 80;
    server_name krishna.yegova.store;
    client_max_body_size 50M;

    location / {
        proxy_pass http://127.0.0.1:3002;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
        proxy_read_timeout 86400;
    }
}

# ==================== CANAAN TRAVEL HUB ====================
server {
    listen 80;
    server_name canaantravelhub.com www.canaantravelhub.com canaan.yegova.store;
    client_max_body_size 50M;

    location / {
        proxy_pass http://127.0.0.1:3003;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
        proxy_read_timeout 86400;
    }
}
