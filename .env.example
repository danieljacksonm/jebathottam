# Copy this file to .env.local for local development
# For Vercel: set these in Project Settings → Environment Variables

# Database (required for Vercel - use MySQL 8.0 or MariaDB 10.x compatible host)
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=ministry_platform

# Set to "true" when using PlanetScale or any SSL-required MySQL host
DB_SSL=false

# JWT secret - use a long random string in production (min 32 characters)
JWT_SECRET=your-super-secret-jwt-key-change-in-production

# Public API base URL (optional - defaults to /api; set for production)
NEXT_PUBLIC_API_URL=https://your-project.vercel.app/api

# Node environment (Vercel sets this automatically)
# NODE_ENV=production
